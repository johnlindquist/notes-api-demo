---
description: 
globs: 
alwaysApply: true
---

**Project Configuration & Tooling Overview:**

*   **Package Management:** Uses `pnpm`. See `package.json` and `pnpm-lock.yaml`.
*   **Build Tool:** Uses Vite (`vite.config.ts`).
*   **Language:** TypeScript (`tsconfig.json`).
*   **Deployment:** Cloudflare Workers (`wrangler.jsonc`).
*   **Version Control:** Git (`.gitignore`).